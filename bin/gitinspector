#!/bin/bash
#
# Shell wrapper for gitinspector Python tool
# This script allows gitinspector to be executed from anywhere when added to PATH

SCRIPT_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec python3 "$SCRIPT_DIR/../gitinspector.py" "$@"
